[package]
name = "pure-jpegli"
description = "Pure-Rust JPEG Lossless codec (ITU-T T.81 Annex H, DICOM Process 14 Selection Value 1)"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true
authors.workspace = true
repository.workspace = true
homepage.workspace = true
keywords = ["jpeg", "lossless", "codec", "dicom", "compression"]
categories = ["compression", "multimedia::images"]

# Published as `pure-jpegli` (the plain `jpegli` name is a native C wrapper on
# crates.io), but the crate is still imported as `jpegli`.
[lib]
name = "jpegli"

[dependencies]
thiserror.workspace = true

[dev-dependencies]
proptest.workspace = true
