# Generated by cargo
# Learn more about Cargo at: https://doc.rust-lang.org

# Rust files -- workspace target plus every nested cargo-fuzz target dir
/target/
target/
**/fuzz/target/
**/fuzz/corpus/
**/fuzz/artifacts/

# OS-specific files
.DS_Store
.env
.history
.claude/worktrees
*.local.json

# Sensitive files
*.pem

# intermediate from blender to dicos
voxels.raw

# Curated DICOS test samples under testdata/ are tracked (the reader and
# luggage integration tests depend on testdata/bag_ct.dcs). Only the
# CI-generated synthetic fixture is ignored.
/testdata/synthetic/

# Large release-only demo asset; distributed as a GitHub release attachment,
# not committed (README links to the v2.0.0 release download).
/roxel-lrg.gif

